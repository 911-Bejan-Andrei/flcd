var a: int;
var i: int;
var is_prime: bool = true;
in(a);
repeat(i=2:i<=a/2:i++) do {
if(a%i:=0) then {
is_prime = false;
break;
}
}
if (is_prime) then {
out(<a> #is a prime number#);
} else {
out(<a> #is not a prime number#);
}